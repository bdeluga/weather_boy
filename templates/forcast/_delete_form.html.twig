<form method="post" action="{{ path('app_forcast_delete', {'id': forcast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forcast.id) }}">
    <button class="btn">Delete</button>
</form>
